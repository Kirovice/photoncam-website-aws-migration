# SC08 接口与数据依赖调查 / API・データ依存調査

调查日期 / 調査日：2026-09-24（Asia/Tokyo）

## 1. 目的与状态 / 目的と状態

中文：判断 `/photoncam/` 迁移是否还需要业务后端、数据库或第三方服务。源码与发布包静态检查已完成；浏览器资源观察已完成一轮，但完整 Network/HAR 抓包尚未完成。开发者确认问题已整理，尚未发送或收到回答。SC08 不标记为最终确认。

日本語：`/photoncam/` の移行に業務バックエンド、データベース、外部サービスが必要かを調べる。ソース・配布物の静的確認とブラウザー資産観察を実施した。完全な Network/HAR 記録は未取得。開発者向け質問は作成済みだが未送信・未回答であり、SC08 は最終承認扱いにしない。

## 2. 输入与方法 / 入力と方法

- 中文：私有源码工作目录 `../PhotonCam.Web-dev`，提交 `572c5a1311151acd45a361aa718f59070bfde32d`，本轮检查时工作树干净。检查配置、依赖清单、页面、工具函数、文档与翻译；未运行旧部署脚本。
- 日本語：上記コミットのソース設定・依存一覧・ページ・補助関数・文書・翻訳を確認。作業ツリーは変更なし。旧デプロイスクリプトは実行していない。
- 中文：只读扫描 `PhotonCam-Web-build.zip` 内全部 50 HTML、72 JS、2 CSS，排除 macOS 元数据。搜索接口、表单、存储、脚本和外部地址，人工检查关键命中的上下文；这不是形式化安全审计或混淆代码的完整证明。
- 日本語：ZIP 内の HTML 50、JS 72、CSS 2 を読み取り専用で検索し、主要な一致箇所を文脈確認した。macOS メタデータは除外。形式的な安全性監査や難読化コードの完全証明ではない。
- 中文：浏览器实际打开线上站点，操作首页轮播、站内导航和语言切换，通过 pageAssets 的 resource 记录及页面 DOM 观察资源、脚本、表单和 iframe。该工具不提供完整网络请求方法、状态码、请求正文、WebSocket 流量或 HAR；尝试读取 Performance 网络记录时，当前工具环境未开放该对象。
- 日本語：実サイトを開いてカルーセル・内部リンク・言語切替を操作し、pageAssets の resource 記録と DOM を確認した。完全な HTTP メソッド・ステータス・本文・WebSocket・HAR は取得できず、Performance API もこのツール環境では利用できなかった。

## 3. 检查结果与依据 / 結果と根拠

| 项目 / 項目 | 结果、依据与解释 / 結果・根拠・説明 |
|---|---|
| 业务 API / 業務API | 自有页面代码中未发现 fetch、axios、XHR、WebSocket、EventSource 或 sendBeacon 业务调用。配置未声明额外 scripts、第三方分析插件或业务服务地址。业务后端未在收到的源码中出现，不等于证明服务器上不存在其他程序。自作ページに業務通信は見当たらず、受領ソース外のサーバー処理は未確認。 |
| 表单提交 / フォーム送信 | 源码及全部发布 HTML 未发现 form；所查浏览器页面也没有 form 或 iframe。相册的 18 个下载链接指向本站 JPG，属于静态文件下载。アップロード・送信フォームは見当たらず、写真ダウンロードは静的 JPG へのリンク。 |
| 业务数据保存 / 業務データ保存 | 未发现自有业务代码将用户内容写入数据库、IndexedDB、localStorage 或 sessionStorage。首页状态用于滚动、动画和轮播；相册说明来自构建内的 JSON，图片来自本地静态资源。状態は表示制御、写真情報はビルド内 JSON・画像資産。 |
| 框架本地存储 / フレームワーク保存 | 编译后的 HTML 会读取 localStorage 中的 theme；主 JS 含 Docusaurus storage 包装和文档版本持久化配置。因此不能写“网站完全不使用本地存储”。本轮未直接读取浏览器存储或证明哪些键实际发生写入。テーマ読取・保存機構があり、実際の書込キーは未確認。 |
| 框架请求 / フレームワーク通信 | 两种语言主 JS 含 XMLHttpRequest GET 的预取兼容代码，周边逻辑是 Docusaurus 路由资源预加载。文本 fetch( 命中来自 prefetch，并非自有 fetch 接口调用。该代码存在不代表本次浏览器实际走过 XHR 分支。コード存在と実際の実行は区別する。 |
| 第三方脚本 / 外部スクリプト | 全部发布 HTML 的外链 script src 只有本站两种语言的 main/runtime 脚本；所查浏览器页面相同，另有框架内联脚本。未发现网页自动加载统计、广告或支付脚本的证据。外部SDKの自動ロードは確認されていない。 |
| 外部链接 / 外部リンク | App Store、社交网站、GitHub HALD 图片、外部 LUT 转换工具、维基百科、Apple 文档和博客是跳转链接。保留链接并在后续验收检查；外部 LUT 工具自身不是本次迁移对象。リンク先の機能を本サイト実装と混同しない。 |
| App说明 / App説明 | privacy.md 的行为统计说明指 App；Subscriptions.md 描述 Apple ID 中管理订阅。教程中的照片保存、导入和转换也不证明本网站具备对应后端。App向け説明とWeb実装を区別する。 |

关键源文件 / 主な根拠：`docusaurus.config.js`、`package.json`、`src/pages/index.js`、`src/pages/gallery.js`、`src/utils/imageUtils.ts`、`src/data/sampleMetadata.json`、`src/pages/privacy.md`、`src/pages/Subscriptions.md`、`docs/CovnertXMPToCube.md` 及中文对应内容。源码仅保存在私有仓库，本文不复制源码。

## 4. 浏览器观察范围 / ブラウザー観察範囲

以下为每次读取时工具返回的资源清单数量；SPA 页面切换会保留先前已观察资源，不能相加作为独立请求总数，也不代表全部请求成功。

以下は観察時の資産一覧件数。SPA 遷移では以前の資産を含むため、合計を実リクエスト数や成功数として扱わない。

| 页面/操作 / ページ・操作 | 清单数量 / 件数 | 观察到的资源主机 / ホスト | form / iframe |
|---|---:|---|---|
| `/photoncam/zh-hans/`，点击 Next 后 / Next操作後 | 32 | juniperphoton.dev | 0 / 0 |
| `/photoncam/zh-hans/gallery` | 50 | juniperphoton.dev | 0 / 0 |
| `/photoncam/zh-hans/docs/intro` | 68 | juniperphoton.dev | 0 / 0 |
| `/photoncam/zh-hans/docs/CovnertXMPToCube` | 70 | juniperphoton.dev | 0 / 0 |
| 切换语言至 / 言語切替 `/photoncam/docs/CovnertXMPToCube/` | 32 | juniperphoton.dev | 0 / 0 |
| `/photoncam/` | 48 | juniperphoton.dev | 0 / 0 |

中文：观察到的是本站图片、CSS、JS 与预加载资源。最后读取的浏览器错误/警告日志为空。未点击外站、未提交数据、未实测全部 50 页、全部图片加载或原图下载；也未检查所有设备、网络与访问条件。因此第2项是部分完成，不能据此宣称完整网络审计通过。

日本語：同一ホストの画像・CSS・JS・先読み資産を観察し、最後のエラー／警告ログは空だった。外部サイト・全50ページ・全画像・実ダウンロード・全端末条件は未検証。第2項目は部分完了であり、完全なネットワーク監査完了とはしない。

## 5. 开发者待确认 / 開発者への確認事項

1. 中文：Linode 上 `/photoncam/` 是否只是静态目录？是否存在源码外的 API、数据库、反向代理、定时任务或特殊重写/响应头配置？请提供相关配置的脱敏片段即可，不需要密码或密钥。
   日本語：対象パスは静的ディレクトリのみか。ソース外の API・DB・プロキシ・定期処理・rewrite・ヘッダー設定があれば、機密を除いた該当設定を確認したい。
2. 中文：是否通过服务器、CDN或其他管理平台额外插入统计脚本？是否有访问日志统计或其他依赖需要保留？源码中的 App 隐私条款是否只描述 App 的统计？
   日本語：サーバー・CDN等から追加する計測処理や維持すべきログ分析はあるか。プライバシー説明の計測は App のみか。
3. 中文：App 是否直接读取该站点的教程、隐私页、订阅说明、图片或 JSON？仓库根部 tutorials.json 由谁读取、如何发布、线上地址是什么？当前未找到网站源码对该文件的引用，不能自行认定它没有用途。
   日本語：App がサイトの文書・画像・JSON を直接参照するか。ルートの tutorials.json の利用者・公開方法・URL は何か。Web側に参照が見つからないことだけで不要とは判断しない。
4. 中文：目前线上版本由哪个源码提交和构建环境生成？源码清单、锁文件与发布包的 Docusaurus 版本不一致，需确认正式迁移基线及依赖版本。
   日本語：本番配布物の元コミット・ビルド環境は何か。依存定義・lock・配布物の版差分を解消するため、移行基準を確認したい。

## 6. 对要件的影响与后续 / 要件への影響と次工程

中文：现有证据支持继续按静态网站准备迁移方案，暂未发现必须新增业务数据库或 API 服务的依据。框架脚本、图片、下载、站内路由、语言切换与外部链接都应保留。开发者回复和完整请求记录补齐后，再关闭 SC08 待确认；若发现 App 直接读取网页资源，应将 URL/响应兼容纳入验收，但不将 App 开发扩入本次范围。Excel 本轮未修改，AWS、DNS 与源码未变更。

日本語：現時点では静的配信を前提に検討を進められ、業務DB/API新設の根拠は未検出。資産・ダウンロード・ルーティング・言語・外部リンクを維持する。開発者回答と完全な通信記録で未決事項を閉じる。Appからの参照があればURL・応答互換を受入条件へ追加するが、App開発自体は対象外。Excel・AWS・DNS・ソースは変更していない。
